# ============================================
# ENVIRONMENT VARIABLES FOR VERCEL DEPLOYMENT
# ============================================
# Copy these to your Vercel project:
# Vercel Dashboard > Project > Settings > Environment Variables

# Database (REQUIRED)
# Get a free PostgreSQL database from:
# - Vercel Postgres (easiest): Click "Storage" tab in your project
# - Neon: https://neon.tech (free tier)
# - Supabase: https://supabase.com (free tier)
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# Authentication (REQUIRED)
# Generate a random secret with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters-long"
NEXTAUTH_SECRET="another-secret-key-for-next-auth-sessions"

# ============================================
# OPTIONAL VARIABLES (add later if needed)
# ============================================

# Redis for caching/sessions (optional)
# REDIS_URL="redis://localhost:6379"

# Stripe for payments (optional)
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."

# OpenAI for AI features (optional)
# OPENAI_API_KEY="sk-..."

# WalletConnect for Web3 (optional)
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID="your-project-id"

